export function SetupChecklist() {
    return <section className="border-line bg-surface rounded-xl border p-5">
    <h2 className="text-ink text-[15px] font-semibold">Try a safe action workflow</h2>
    <p className="text-ink-muted mt-1 text-[13px] leading-relaxed">Choose a fictional message, review or change its suggestions, then approve and simulate an action. The assistant uses built-in rules. No accounts, model downloads or API keys are needed.</p>
    <p className="text-ink-subtle mt-2 text-xs">Everything is held in memory until you reset the demo or restart the server. Nothing is sent or booked.</p>
  </section>;
}
